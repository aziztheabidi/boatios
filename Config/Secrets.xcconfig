// Non-secret placeholders for local builds. Replace with real keys in your local copy only
// (do not commit real credentials). Xcode merges these into Info.plist at build time.
GOOGLE_API_KEY = YOUR_GOOGLE_API_KEY
STRIPE_PUBLISHABLE_KEY = YOUR_STRIPE_KEY
